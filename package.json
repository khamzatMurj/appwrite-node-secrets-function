{
  "name": "appwrite-node-secrets-function",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Appwrite Function that reads three project secrets safely.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test"
  }
}
